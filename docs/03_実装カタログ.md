# 実装カタログ

本ドキュメントは `docs/README.md §3` の更新規約に従い、コード・IaC 変更と同期して維持する。

最終更新: 2026-04-19

---

## 1. ディレクトリ / ファイル構成

```text
study-llm-reranking-mlops/
 ├─ Dockerfile                      # API コンテナビルド定義（python:3.11-slim + libgomp1）
 ├─ docker-compose.yml              # ローカル開発 5 サービス定義
 ├─ .env.example                    # 環境変数テンプレート（DB/Meili/Redis/pgAdmin/モデルパス）
 ├─ Makefile                        # 全 make ターゲット定義
 ├─ requirements.txt                # Python 依存パッケージ
 ├─ artifacts/                      # 学習成果物（コンテナマウント）
 │   ├─ models/
 │   │   ├─ lgbm_ranker.txt         # LightGBM 学習済みモデル（LambdaRank）
 │   │   └─ lgbm_ranker_metadata.json  # モデルバージョン・学習日時・行数等メタデータ
 │   ├─ train/
 │   │   └─ rank_train.csv          # 学習データ CSV（query_id, property_id, features, label）
 │   └─ reports/
 │       └─ phase6_weekly_eval_YYYYMMDD.{csv,md}  # 週次評価レポート
 ├─ src/
 │   ├─ api/
 │   │   ├─ main.py                 # FastAPI アプリ初期化・ルーター登録・/health
 │   │   └─ routes/
 │   │       ├─ search.py           # GET /search — Meili 取得 → ME5 再ランク → LightGBM 再ランク → 返却
 │   │       └─ feedback.py        # POST /feedback — click/favorite/inquiry ログ保存
 │   ├─ clients/
 │   │   ├─ meilisearch_client.py  # Meilisearch HTTP クライアントラッパー（タスク完了待ちポーリング付き）
 │   │   └─ redis_client.py        # Redis キャッシュクライアント（/search 結果の TTL キャッシュ）
 │   ├─ services/
 │   │   ├─ search/
 │   │   │   ├─ property_search_service.py  # 検索メイン処理（Meili→ME5→LightGBM 再ランク）
 │   │   │   └─ query_filter_builder.py    # Meili 検索ペイロード（フィルタ/ソート）組み立て
 │   │   ├─ ranking/
 │   │   │   ├─ feature_service.py         # property_features 読み書き・stats 再集計・inactive 除外
 │   │   │   └─ lgbm_reranker.py           # LightGBM モデルロードとスコアリング（rerank_with_lgbm）
 │   │   ├─ embeddings/
 │   │   │   ├─ me5_embedding_service.py   # ME5（multilingual-e5-large）埋め込み生成（fallback あり）
 │   │   │   └─ similarity_service.py      # cosine_similarity 計算
 │   │   └─ evaluation/
 │   │       ├─ offline_metrics_service.py # NDCG@10 / MAP / Recall@20 計算ロジック
 │   │       └─ kpi_service.py             # KPI 比率計算の共通関数
 │   ├─ trainers/
 │   │   ├─ training_dataset_builder.py    # 学習データ CSV 生成（search_logs JOIN 特徴量 → label 付与）
 │   │   └─ lgbm_trainer.py               # LightGBM LambdaRank 学習・モデル保存・メタデータ記録
 │   ├─ core/
 │   │   └─ db.py                          # psycopg3 接続プール（ConnectionPool・get_db_connection）
 │   ├─ repositories/
 │   │   ├─ search_log_repository.py       # search_logs 保存・impressions 更新・feedback 適用
 │   │   ├─ property_embedding_repository.py # property_embeddings テーブル読み書き
 │   │   ├─ ranking_compare_repository.py  # ranking_compare_logs 保存（log_ranking_comparison）
 │   │   └─ evaluation_report_repository.py # offline_eval_reports / model_adoption_decisions の保存・参照
 │   ├─ jobs/
 │   │   ├─ maintenance/
 │   │   │   └─ run_migrations.py          # マイグレーション SQL ファイルを DB に流すユーティリティ
 │   │   ├─ indexing/
 │   │   │   └─ sync_properties_to_meilisearch.py  # DB → Meilisearch 全件同期バッチ
 │   │   ├─ features/
 │   │   │   ├─ aggregate_daily_property_stats.py   # 日次バッチ（CTR 再集計 / 人気物件更新 / inactive 除外）
 │   │   │   └─ export_feature_report.py            # 特徴量サマリレポート出力（デバッグ用）
 │   │   ├─ embeddings/
 │   │   │   └─ generate_property_embeddings.py     # active 物件の ME5 embedding 事前計算バッチ
 │   │   ├─ evaluation/
 │   │   │   ├─ run_offline_evaluation.py           # NDCG/MAP/Recall 計算 → offline_eval_reports 保存
 │   │   │   ├─ aggregate_daily_kpi.py              # 日次 KPI 集計 → kpi_daily_stats 保存（--date オプション）
 │   │   │   ├─ export_ranking_compare_report.py    # ranking_compare_logs サマリ出力
 │   │   │   └─ export_weekly_evaluation_report.py  # 週次評価レポート CSV+MD 出力 + 採用判定 → model_adoption_decisions
 │   │   └─ training/
 │   │       └─ run_weekly_retraining.py            # 採用判定に基づく週次再学習実行
 │   └─ migrations/                        # DDL マイグレーション SQL（番号順に適用）
 │       ├─ 001_create_properties.sql
 │       ├─ 002_seed_properties.sql
 │       ├─ 003_create_logs_and_stats.sql
 │       ├─ 004_phase3_features_and_batch_logs.sql
 │       ├─ 005_phase4_me5.sql
 │       ├─ 006_phase5_learning_logs.sql
 │       ├─ 007_phase5_ranking_compare_logs.sql
 │       └─ 008_phase6_eval_and_kpi.sql
 ├─ tests/
 │   ├─ api/
 │   │   └─ test_api.py                    # /health /search /feedback の正常系テスト（/search キャッシュヒット含む）
 │   ├─ clients/
 │   │   └─ test_redis_client.py           # Redis クライアント（キー生成・hit/miss・障害時フォールバック）
 │   └─ services/
 │       ├─ embeddings/
 │       │   └─ test_similarity_service.py
 │       ├─ evaluation/
 │       │   ├─ test_kpi_service.py
 │       │   └─ test_offline_metrics_service.py
 │       └─ search/
 │           └─ test_query_filter_builder.py
 └─ docs/
     ├─ README.md                  # ドキュメント運用ルール（権威順位・更新規約）
     ├─ 01_仕様と設計.md            # 機能仕様・アーキテクチャ設計
     ├─ 02_移行ロードマップ.md       # 決定的仕様・Phase 別実装計画・最終チェックリスト
     ├─ 03_実装カタログ.md           # 本ファイル
     └─ 04_運用.md                  # 環境構築手順・定常運用・インシデント対応
```

---

## 2. DB テーブル一覧とリレーション

### テーブル一覧

| テーブル | 導入 Phase | 主な役割 |
|---|---|---|
| `properties` | Phase 0-1 | 物件マスタ（基本属性） |
| `property_stats` | Phase 2 | 物件ごとの行動集計（impression/click/CTR 等） |
| `search_logs` | Phase 2 | 検索セッションログ（クエリ・結果・フィードバック） |
| `property_features` | Phase 3 | ランキング用特徴量（日次更新） |
| `batch_job_logs` | Phase 3 | バッチ実行ログ（成功/失敗/件数） |
| `property_embeddings` | Phase 4 | 物件 ME5 埋め込みベクトル |
| `ranking_compare_logs` | Phase 5 | Meili 順 vs 再ランキング順の差分ログ |
| `offline_eval_reports` | Phase 6 | NDCG/MAP/Recall の評価結果スナップショット |
| `kpi_daily_stats` | Phase 6 | 日次 KPI（impressions/clicks/CTR/CVR 等） |
| `model_adoption_decisions` | Phase 6 | モデル採用/非採用の判定記録 |

### スキーマ詳細

#### `properties`
| カラム | 型 | 制約 | 説明 |
|---|---|---|---|
| id | BIGINT | PK | 物件 ID |
| title | TEXT | NOT NULL | 物件名 |
| description | TEXT | NOT NULL | 説明文（ME5 embedding 対象） |
| city | TEXT | NOT NULL | 市区（例: 札幌市）|
| ward | TEXT | | 区 |
| price | INTEGER | NOT NULL, ≥0 | 賃料（円） |
| management_fee | INTEGER | NOT NULL, DEFAULT 0 | 管理費 |
| layout | TEXT | NOT NULL | 間取り（例: 2LDK） |
| area | NUMERIC(8,2) | NOT NULL, >0 | 専有面積（㎡） |
| age | INTEGER | NOT NULL, ≥0 | 築年数 |
| walk_min | INTEGER | NOT NULL, ≥0 | 最寄り駅徒歩分 |
| pet | BOOLEAN | NOT NULL, DEFAULT FALSE | ペット可否 |
| is_active | BOOLEAN | NOT NULL, DEFAULT TRUE | 掲載中フラグ（Phase 3 追加） |
| created_at | TIMESTAMP | NOT NULL | 登録日時 |

#### `property_stats`
| カラム | 型 | 説明 |
|---|---|---|
| property_id | BIGINT PK FK→properties | |
| impression | BIGINT | 表示回数 |
| click | BIGINT | クリック数 |
| favorite | BIGINT | お気に入り数 |
| inquiry | BIGINT | 問い合わせ数 |
| ctr | DOUBLE | クリック率 |
| fav_rate | DOUBLE | お気に入り率 |
| inquiry_rate | DOUBLE | 問い合わせ率 |
| updated_at | TIMESTAMP | |

#### `search_logs`
| カラム | 型 | 説明 |
|---|---|---|
| id | BIGSERIAL PK | |
| query | TEXT | 検索クエリ文字列 |
| user_id | BIGINT | ユーザー ID（任意） |
| result_ids | JSONB | 返却した物件 ID リスト |
| clicked_id | BIGINT FK→properties | クリックされた物件（任意） |
| me5_scores | JSONB | 各物件の me5_score（Phase 4 追加） |
| actioned_id | BIGINT FK→properties | フィードバック対象物件（Phase 5 追加） |
| action_type | TEXT | click/favorite/inquiry（Phase 5 追加） |
| created_at | TIMESTAMP | |

#### `property_features`
| カラム | 型 | 説明 |
|---|---|---|
| property_id | BIGINT PK FK→properties | |
| price | INTEGER | 賃料 |
| walk_min | INTEGER | 駅徒歩分 |
| age | INTEGER | 築年数 |
| area | DOUBLE | 専有面積 |
| photo_count | INTEGER | 写真枚数 |
| ctr | DOUBLE | CTR |
| fav_rate | DOUBLE | お気に入り率 |
| inquiry_rate | DOUBLE | 問い合わせ率 |
| popularity_score | DOUBLE | 人気スコア（複合指標） |
| me5_score | DOUBLE | ME5 cosine 類似度（Phase 4 追加） |
| updated_at | TIMESTAMP | |

#### `batch_job_logs`
| カラム | 型 | 説明 |
|---|---|---|
| id | BIGSERIAL PK | |
| job_name | TEXT | バッチ識別子 |
| status | TEXT | success / failure |
| processed_count | INTEGER | 処理件数 |
| error_message | TEXT | エラー詳細（任意） |
| started_at | TIMESTAMP | |
| finished_at | TIMESTAMP | |

#### `property_embeddings`
| カラム | 型 | 説明 |
|---|---|---|
| property_id | BIGINT PK FK→properties | |
| model_name | TEXT | 使用モデル名（例: intfloat/multilingual-e5-large） |
| embedding | DOUBLE[] | 埋め込みベクトル |
| updated_at | TIMESTAMP | |

#### `ranking_compare_logs`
| カラム | 型 | 説明 |
|---|---|---|
| id | BIGSERIAL PK | |
| search_log_id | BIGINT FK→search_logs | |
| meili_result_ids | JSONB | Meili 返却順の物件 ID リスト |
| reranked_result_ids | JSONB | 再ランキング後の物件 ID リスト |
| changed_count | INTEGER | 順位変動件数 |
| top1_changed | BOOLEAN | 1 位が変わったか |
| created_at | TIMESTAMP | |

#### `offline_eval_reports`
| カラム | 型 | 説明 |
|---|---|---|
| id | BIGSERIAL PK | |
| evaluated_queries | INTEGER | 評価クエリ数 |
| ndcg10_meili / ndcg10_lgbm | DOUBLE | NDCG@10（Meili順 / LightGBM順） |
| map_meili / map_lgbm | DOUBLE | MAP（同上） |
| recall20_meili / recall20_lgbm | DOUBLE | Recall@20（同上） |
| created_at | TIMESTAMP | |

#### `kpi_daily_stats`
| カラム | 型 | 説明 |
|---|---|---|
| stat_date | DATE PK | 集計対象日 |
| impressions | BIGINT | 検索結果表示数 |
| clicks | BIGINT | クリック数 |
| favorites | BIGINT | お気に入り数 |
| inquiries | BIGINT | 問い合わせ数 |
| ctr | DOUBLE | クリック率 |
| favorite_rate | DOUBLE | お気に入り率 |
| inquiry_rate | DOUBLE | 問い合わせ率 |
| cvr | DOUBLE | コンバージョン率（inquiry/impression） |
| created_at / updated_at | TIMESTAMP | |

#### `model_adoption_decisions`
| カラム | 型 | 説明 |
|---|---|---|
| id | BIGSERIAL PK | |
| evaluated_at | TIMESTAMP | 判定日時 |
| adopt_lgbm | BOOLEAN | LightGBM 採用可否 |
| reason | TEXT | 不採用理由または "adopted" |
| thresholds | JSONB | 判定時の閾値設定スナップショット |
| metrics | JSONB | 判定時の指標値スナップショット |

### リレーション図（簡易）

```
properties
  ├─ property_stats       (1:1)
  ├─ property_features    (1:1)
  ├─ property_embeddings  (1:1)
  └─ search_logs          (1:N, clicked_id / actioned_id)
       └─ ranking_compare_logs (1:1)

offline_eval_reports      (独立)
kpi_daily_stats           (独立)
model_adoption_decisions  (独立)
batch_job_logs            (独立)
```

---

## 3. API エンドポイント

### `GET /health`

| 項目 | 内容 |
|---|---|
| 実装 | `src/api/main.py` |
| 説明 | ヘルスチェック |
| レスポンス | `{"status": "ok"}` |

---

### `GET /search`

| 項目 | 内容 |
|---|---|
| 実装 | `src/api/routes/search.py` |
| 説明 | 物件検索（Redis キャッシュ確認 → Meili → ME5 再ランク → LightGBM 再ランク） |

**クエリパラメータ**

| パラメータ | 型 | デフォルト | 説明 |
|---|---|---|---|
| q | str | `""` | 全文検索クエリ |
| user_id | int | null | ユーザー ID（ログ紐付け用） |
| city | str | null | 市区フィルタ |
| layout | str | null | 間取りフィルタ（例: 2LDK） |
| price_lte | int | null | 賃料上限（円） |
| pet | bool | null | ペット可フィルタ |
| walk_min | int | null | 駅徒歩分上限 |
| limit | int | 20 | 返却件数（1〜100） |
| candidate_limit | int | 100 | Meili 取得候補数（1〜200） |

**レスポンス例**

```json
{
  "search_log_id": 42,
  "compare_log_id": 7,
  "count": 20,
  "items": [
    {
      "id": 1,
      "title": "...",
      "price": 85000,
      "me5_score": 0.074675,
      "lgbm_score": 0.357792,
      ...
    }
  ]
}
```

---

### `POST /feedback`

| 項目 | 内容 |
|---|---|
| 実装 | `src/api/routes/feedback.py` |
| 説明 | ユーザー行動ログ記録（click/favorite/inquiry） |

**リクエストボディ**

```json
{
  "user_id": 1,
  "property_id": 5,
  "action": "favorite",
  "search_log_id": 42
}
```

| フィールド | 型 | 必須 | 説明 |
|---|---|---|---|
| user_id | int | 任意 | ユーザー ID |
| property_id | int | 必須 | 対象物件 ID |
| action | str | 必須 | `click` / `favorite` / `inquiry` |
| search_log_id | int | 任意 | 紐付ける検索ログ ID |

**レスポンス例**

```json
{
  "status": "ok",
  "property_id": 5,
  "action": "favorite",
  "search_log_updated": true
}
```

---

## 4. バッチジョブ一覧

| ファイル | make ターゲット | 実行契機 | 説明 |
|---|---|---|---|
| `jobs/indexing/sync_properties_to_meilisearch.py` | `make phase1-sync` | 物件データ変更時 | DB → Meilisearch 全件同期 |
| `jobs/features/aggregate_daily_property_stats.py` | `make phase3-daily` | 日次 | CTR 再集計・人気物件更新・inactive 除外 |
| `jobs/embeddings/generate_property_embeddings.py` | `make phase4-generate-embeddings` | 物件追加時・日次 | active 物件の ME5 embedding 事前計算 |
| `jobs/features/export_feature_report.py` | `make phase3-feature-check` | デバッグ時 | 特徴量サマリ出力 |
| `jobs/evaluation/export_ranking_compare_report.py` | `make phase5-compare-check` | 確認時 | Meili vs 再ランキング差分サマリ |
| `jobs/evaluation/run_offline_evaluation.py` | `make phase6-offline-eval` | 週次 | NDCG/MAP/Recall 計算・offline_eval_reports 保存 |
| `jobs/evaluation/aggregate_daily_kpi.py` | `make phase6-kpi-daily` | 日次 | KPI 日次集計・kpi_daily_stats 保存 |
| `jobs/evaluation/export_weekly_evaluation_report.py` | `make phase6-weekly-report` | 週次 | 週次評価レポート出力 + モデル採用判定 |
| `jobs/training/run_weekly_retraining.py` | `make phase6-weekly-retrain` | 週次 | 採用判定に基づく再学習実行 |
| `jobs/maintenance/run_migrations.py` | `make *-migrate` | マイグレーション時 | SQL ファイルを DB に適用するユーティリティ |

---

## 5. Docker Compose サービス構成

| サービス | イメージ | ポート | 説明 |
|---|---|---|---|
| `api` | `./Dockerfile`（python:3.11-slim + libgomp1） | 8000 | FastAPI アプリ本体 |
| `postgres` | `postgres:16` | 5432 | メイン DB（DB名: estate） |
| `pgadmin` | `dpage/pgadmin4` | 5050 | DB 管理 UI |
| `meilisearch` | `getmeili/meilisearch:v1.7` | 7700 | 全文検索エンジン |
| `redis` | `redis:7` | 6379 | `/search` 結果の短期キャッシュ（TTL: `SEARCH_CACHE_TTL_SECONDS`） |

---

## 6. アーティファクト / 環境変数

### モデル成果物パス（コンテナ内）

| パス | 内容 |
|---|---|
| `/app/artifacts/models/lgbm_ranker.txt` | LightGBM 学習済みモデル |
| `/app/artifacts/models/lgbm_ranker_metadata.json` | バージョン・学習日時・学習行数等 |
| `/app/artifacts/train/rank_train.csv` | 学習データ CSV |
| `/app/artifacts/reports/phase6_weekly_eval_YYYYMMDD.csv` | 週次評価レポート CSV |
| `/app/artifacts/reports/phase6_weekly_eval_YYYYMMDD.md` | 週次評価レポート Markdown |

### 主要環境変数（`.env.example` より）

| 変数 | デフォルト | 説明 |
|---|---|---|
| `POSTGRES_HOST` | `postgres` | DB ホスト |
| `POSTGRES_DB` | `estate` | DB 名 |
| `POSTGRES_USER` | `admin` | DB ユーザー |
| `POSTGRES_PASSWORD` | `<change_me>` | DB パスワード |
| `MEILI_HTTP_ADDR` | `meilisearch:7700` | Meilisearch アドレス |
| `MEILI_INDEX` | `properties` | Meilisearch インデックス名 |
| `ME5_MODEL_NAME` | `intfloat/multilingual-e5-large` | ME5 モデル識別子 |
| `PHASE6_MIN_EVAL_QUERIES` | `10` | 採用判定の最低評価クエリ数 |
| `PHASE6_MIN_NDCG_GAIN` | `0.02` | NDCG@10 改善閾値 |
| `PHASE6_MIN_MAP_GAIN` | `0.01` | MAP 改善閾値 |
| `PHASE6_REQUIRE_RECALL_NON_DECREASE` | `true` | Recall@20 非劣化を採用条件とするか |
| `REDIS_HOST` | `redis` | Redis ホスト |
| `REDIS_PORT` | `6379` | Redis ポート |
| `SEARCH_CACHE_TTL_SECONDS` | `120` | `/search` キャッシュ TTL（秒） |

---

## 7. 未着手（Phase 7）

以下は Phase 7 として計画中。現時点では未実装。

- Docker イメージ分割（api / batch）
- Cloud Run または ECS/Fargate デプロイ設定
- マネージド DB（Cloud SQL / Aurora）接続設定
- シークレット管理（Secret Manager / Parameter Store）
- リリース手順書・障害対応手順書

---

## 8. テスト構成

テストは `src/` の責務境界に対応して分離している。

| テストパス | 主対象 | 備考 |
|---|---|---|
| `tests/api/test_api.py` | FastAPI エンドポイント | `/search` のキャッシュミス/ヒット双方を検証 |
| `tests/clients/test_redis_client.py` | Redis クライアント | 例外時に API を落とさないフォールバック方針を検証 |
| `tests/services/search/test_query_filter_builder.py` | 検索クエリ組み立て | Meili フィルタ条件の正常系を検証 |
| `tests/services/evaluation/test_kpi_service.py` | KPI 算出ロジック | ゼロ除算を含む境界ケースを検証 |
| `tests/services/evaluation/test_offline_metrics_service.py` | オフライン評価指標 | NDCG/MAP/Recall の計算を検証 |
| `tests/services/embeddings/test_similarity_service.py` | 類似度計算 | cosine 類似度の基本特性を検証 |
